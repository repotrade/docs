# legacy poetry
[tool.poetry]
package-mode = false

[project]
name = "docs"
version = "0.1.0"
description = "docs.repo.trade"
authors = [{ name = "fabian@chainsquad.com" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs (>=1.6.1,<2.0.0)",
    "mkdocs-gen-files (>=0.5.0,<0.6.0)",
    "mkdocs-material (>=9.6.9,<10.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
